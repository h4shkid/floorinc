hey team, quick update on the forecasting platform

thanks to liz i got access to netsuite live data and updated the website with real data. here's what's new:

- the platform now pulls directly from netsuite — 21k+ skus and 410k+ sales transactions across all channels
- added a data sync tab where you can manually trigger a sync or it runs automatically every 24 hours
- added a stats bar below the header showing total skus, transactions, active skus, channels, and date range so you can confirm what data you're looking at
- added manufacturer/vendor info for each sku — you can now filter and sort by manufacturer
- the forecast engine uses velocity, seasonality, and lead times to classify every sku as backorder, red (order now), yellow (getting close), or green (comfortable)

take a look when you get a chance and let me know your feedback — what's useful, what's missing, what would make it more helpful for your workflow
